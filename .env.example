# Copy this file to .env and fill in your values.
# .env is loaded automatically at startup — do NOT commit .env to version control.

# ---------------------------------------------------------------------------
# Groq API (required for the live Llama-3-8B judge)
# Free tier: https://console.groq.com
# Leave blank if you plan to use --skip-llm (GPT-4 labels from the dataset).
# ---------------------------------------------------------------------------
GROQ_API_KEY=your_groq_api_key_here
